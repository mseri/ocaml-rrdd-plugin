(library
  ((name rrdd_plugin)
   (public_name xapi-rrdd-plugin)
   (flags (:standard -bin-annot))
   (wrapped false)
   (libraries (forkexec
               xapi-stdext-pervasives
               xapi-stdext-std
               xapi-stdext-threads
               xapi-stdext-unix
               threads
               xapi-rrd
               xapi-rrd-transport
               xcp.rrd
               xenstore_transport.unix
               xenstore.unix))
  ))
